# V1 RELEASE AUDIT

> 2026-08-25 · branch `feat/v1-monetization-foundation` · 기준 커밋 `a14419d`
> **갱신 2026-08-25** (`abeaf31` 이후): A1/A2/B1/B2/B3/B4/B5/B6/C6 해소, A5/B7은 초안 확보 — 아래 표에 표시했다.
> 설정 항목은 이제 `npm run verify:release`(22개)가 지킨다 — 식별자·권한·EAS 프로필·V1 경계가 틀어지면 검증이 실패한다.
> **새 기능을 구현하지 않았다.** 저장소의 현재 상태만 조사하고 분류했다.
> 관련: [PROJECT_STATE.md](PROJECT_STATE.md) · [DECISION_LOG.md](DECISION_LOG.md) · [FAILURE_LOG.md](FAILURE_LOG.md)

## 방법

실행한 것과 실행하지 못한 것을 구분한다. 확인하지 못한 것은 **MANUAL QA**로 뺐고, 추측으로 판정하지 않았다.

| 실행 | 결과 |
| --- | --- |
| `npx tsc --noEmit` | PASS |
| `npm run lint` | PASS |
| verify 스크립트 15종 | PASS (1,020 단언, FAIL 0 — `verify:release` 25개, `verify:storage` 197개, `verify:pt` 73개 포함) |
| `npx expo config --type public` | 해석된 설정 확인 (아래 A/B 항목의 근거) |
| `npx expo export --platform android` | **성공** — production JS 번들 생성, exit 0. 설정 변경·의존성 정리·버전 정렬 뒤에도 매번 다시 확인했다(현재 4.4MB) |
| `npx expo-doctor` | **21/21 통과** (최초 조사에서는 패치 버전 불일치 7건으로 1개 실패했다) |

확인하지 못한 것: 네이티브 빌드(EAS 계정 필요), 실기기 동작, 스토어 콘솔, 아이콘/스플래시의 시각적 최종 여부.
번들 안에서 mock 코드가 제거됐는지 문자열로 확인하려 했으나 **Hermes 바이트코드라 문자열 검색이 통하지 않았다**(반드시 있어야 할 문자열도 검색되지 않음). 따라서 mock 제외 근거는 소스의 `__DEV__` 경계와 `npm run verify:monetization`까지다.

## 요약

| 분류 | 최초 조사 | 현재 남은 것 |
| --- | --- | --- |
| A. 출시 차단 (BLOCKER) | 5 | **2** (A4 스토어 계정 · A5 개인정보처리방침) |
| B. 출시 전 필수 (REQUIRED) | 8 | **2** (B7 데이터 안전 콘솔 입력 · B8 크래시 리포팅) |
| C. 출시 후 가능 (POST-LAUNCH) | 6 | **5** (C6 README 해소) |
| D. 이미 완료 (DONE) | 9 | 9 |
| E. MANUAL QA | 6 | 6 |

남은 출시 차단 3건은 **전부 계정·문서 쪽이며 AI가 대신할 수 없다**(A3·A4는 계정, A5는 게시 URL이 필요하다).

### 출시 전 품질 점검 (2026-08-25, 코드 기준)

설정 항목 밖에서도 한 번 훑었다. 실제로 고친 것은 하나다.

| 확인 | 결과 |
| --- | --- |
| 사용자에게 사실과 다른 안내 | **1건 발견·수정** — 설정 화면이 출시 빌드에서 "광고를 보면 AI PT를 이용할 수 있어요"라고 안내했지만 그 빌드에는 보상 어댑터가 없다. AI PT 화면과 같은 문구를 쓰도록 고쳤다(commit `8a833d2`) |
| 디버그 로그 잔재 | `src`에 `console.*` **0건** |
| 남은 `TODO` 9건 | 전부 향후 확장 표시(3D 모델, 부위별 파라미터 등)이며 사용자에게 보이지 않는다 |
| 빈 상태 문구 | 히스토리/운동 탭 모두 정상 안내 문구 |

### 브라우저 스모크 테스트 (2026-08-25)

**실기기가 아니다.** `npx expo start --web`으로 띄운 현재 코드에서 핵심 루프를 실제로 클릭해 봤다.
E 항목(실기기 QA)을 대신하지 못하지만, 최근 변경(PR 두 종류 · XP 분리 · 사진 복사 경로 · 의존성
정렬 · 설정 문구)이 앱을 깨뜨리지 않았다는 실행 근거는 된다.

확인한 흐름: 온보딩 4단계(성별·키/체중·체형·목표) → 홈 → [운동 시작] → 오늘 추천(하체) →
세션 시작 → 세트 2개 기록(60kg×10, 65kg×8) → 자동 휴식 → 운동 종료 → 결과.

| 확인 | 결과 |
| --- | --- |
| 콘솔 에러 | **0건** |
| PR 표시 | 세트 완료마다 `🏆 NEW PR 스쿼트 60kg` → `65kg`. 결과 화면 `스쿼트 65kg (첫 기록)` — 중량 PR에 횟수를 붙이지 않는 새 문구 규칙대로 |
| XP 계산 | `+25 XP` = 세션 10 + 중량 PR 15. 저장값도 `{"xp":25}` |
| 집계 | 총 세트 2, 총 볼륨 1,120kg(60×10 + 65×8), streak 1일 |
| 완료 파이프라인 | 끝까지 돌아 `pendingSessionCompletion`과 `activeWorkoutSession`이 **둘 다 정리됨** |
| 온보딩 역방향 가드 | 브라우저에 남아 있던 옛 운동 기록 1건은 그대로 두고 프로필만 다시 만들었다 — `resolveOnboardingState`가 의도대로 동작 |
| 사진 복사 경로 | 사진 없이 온보딩을 끝내는 경로에서 정상 통과(복사 분기를 타지 않음). 실제 복사는 실기기 확인(`E4`) |

### 2차 브라우저 QA (2026-08-25) — **버그 1건 발견·수정**

첫 스모크 테스트가 훑지 않은 화면과 경로를 눌러 봤다. 순수 함수 검증 990개가 통과하는 상태에서
**화면에서만 드러나는 버그가 하나 나왔다.**

| 확인 | 결과 |
| --- | --- |
| **신체 기록 추가** | **버그 발견 → 수정(`3f29487`)**. 오늘 기록이 이미 있는 상태에서 체중·체지방률·골격근량을 넣었더니 저장은 됐는데 화면은 옛 값(78kg, 나머지 `-`)을 계속 보여줬다. 같은 날짜 정렬 비교자가 ±1만 돌려줘 방금 넣은 기록이 뒤로 밀린 탓이다. 고친 뒤 같은 조작에서 즉시 `77kg / 16.8% / 35.1kg`로 바뀌는 것을 확인했다 |
| **rep PR 전체 경로** | `65kg × 8회` 기록이 있는 상태에서 `65kg × 12회` → 결과 화면 `NEW PR 스쿼트 65kg × 12회 (이전 65kg × 8회)`, **XP +20**(세션 10 + rep PR 10). 중량 PR 세션의 +25와 구분된다 — `DEC-011`이 UI 끝단까지 맞다 |
| **세션 복구(E1의 웹 근사)** | 휴식 중에 페이지를 완전히 새로고침 → 세션·세트·타이머가 살아났고 **남은 휴식이 흘러간 시간만큼 줄어 있었다**(02:58 → 02:44). 절대시각 기반 휴식이 JS 컨텍스트 재생성에도 어긋나지 않는다 |
| **지난 운동 그대로** | 첫 화면 옵션으로 뜨고, 세션에 `이전: 65kg × 8회 / 최근 최고: 65kg × 8회`가 표시되며 입력값도 지난 세트로 미리 채워졌다 |
| **부위 추천 회전** | 하체를 한 다음 날 추천이 등으로 바뀌었다 |
| **기간 통계** | 히스토리 `PR 2개` — `countPeriodPRs`가 중량 PR과 rep PR을 같은 기준으로 센다 |
| 콘솔 에러 | **0건** |

남은 한계: 고치기 전에 저장된 배열의 뒤집힌 순서는 되돌릴 수 없다(기록에 시각 정보가 없다).
새로 넣는 기록부터 올바른 자리에 들어간다 — 출시 전이라 실사용자 데이터는 없다.

### 3차 브라우저 QA (2026-08-25) — **버그 1건 수정 · 제품 갭 1건 보고**

아직 눌러 보지 않은 화면(운동 탭 · 루틴 편집 · 트레이너)을 확인했다.

| 확인 | 결과 |
| --- | --- |
| **PT가 말하는 PR** | **버그 발견 → 수정(`c18fd05`)**. 65kg를 이미 든 상태에서 65kg × 12회로 횟수 기록을 깼는데 스탠리가 "스쿼트는 **65kg가 첫 기록**입니다"라고 말했다. `DEC-011`의 마무리 누락 — `PtContext`에 `kind`는 실렸지만 `buildPrLine`이 중량 기준으로만 판단했다. 이제 "스쿼트 65kg로 12회, 횟수 기록 넘기셨습니다"로 말하고 맨몸은 "0kg" 대신 "맨몸"이라고 한다 |
| 루틴 만들기/수정 | 이름·요일·운동 순서 저장 정상. 저장값 `{name:'가슴 A', scheduledDays:[1], exerciseIds:[bench-press, incline-dumbbell-press]}`, 목록·상세 표시도 일치 |
| 트레이너 탭 요약 | 실제 기록과 일치 — "이번 주 2회 · 볼륨 1,900kg · 연속 1일", "최근에 스쿼트 65kg × 12회" |
| 신체 수치 반영(2차 수정 확인) | 홈·히스토리 모두 최신 값(77kg / 35.1kg / 16.8%)을 보여준다 |
| 콘솔 에러 | **0건** |

### 4차 브라우저 QA (2026-08-25) — **버그 1건 수정**

| 확인 | 결과 |
| --- | --- |
| **스탠리 문장의 한글 조사** | **버그 발견 → 수정(`281afdb`)**. AI 상담에서 "한동안 **등를** 안 하셨네요"라고 말했다. 조사가 `${label}를`로 문자열에 박혀 있어 받침 있는 부위(가슴·등·팔·전신 — 여섯 중 넷)에서 전부 어긋났다. 운동 이름도 같았다("풀업는", "풀업가"). 규칙은 `profile-validation.ts` 안에 이미 있었지만 갇혀 있어서 `utils/korean.ts`로 꺼내 공용화했다 |
| AI 상담 게이트 | 개발 빌드에서 광고 버튼 + DEV 구독 버튼 + "mock은 출시 빌드에서 인정되지 않아요" 안내가 정상. mock 광고 1회 → 이용권 1회 지급 후 즉시 소비(`rewardedPtUsesRemaining: 0`) |
| 오프라인 PT 답변 | "AI가 아직 연결되지 않았어요 — 기록에 없는 내용은 지어내지 않아요" 고지 후, 저장된 기록만으로 부위 추천 |
| 콘솔 에러 | **0건** |

아직 눌러 보지 않은 화면: 놓친 운동 기록 수동 추가, PASS/알림 화면, 설정의 데이터 초기화.

**제품 갭(수정하지 않음, 판단 필요)**: 저장한 루틴을 **예정 요일이 아닌 날에 시작할 경로가 없다.**
`buildQuickStartPlan`은 `오늘 예정 루틴 ?? 지난번 쓴 루틴`만 후보로 올리고(`utils/workout-start.ts`),
운동 탭에서 루틴을 누르면 `/routine-edit`(수정)로 간다. 월요일 예정으로 만든 루틴은 화요일에
"가슴 A로 시작" 할 방법이 없다. `PRODUCT_SPEC`의 "계획형/즉흥형/추천형을 모두 지원한다"와
어긋나 보이지만, 진입점을 새로 만드는 것은 UX 결정이라 손대지 않았다.

---

## A. 출시 차단 — 이것이 없으면 빌드/제출 자체가 불가능하다

| # | 항목 | 확인된 사실 | 필요한 것 |
| --- | --- | --- | --- |
| A1 | ~~앱 식별자 없음~~ **해소** | 사용자 승인으로 `com.helchanggrow.app`을 iOS/Android 양쪽에 설정했다. `npx expo config`로 확인 | — |
| A2 | ~~EAS 빌드 설정 없음~~ **해소** | `eas.json` 추가 — development / preview(APK, 내부 배포) / production(AAB) 세 프로필. `appVersionSource: "local"`이라 버전 숫자의 원본은 이 저장소다 | 실제 빌드 실행은 A3·A4 이후(MANUAL) |
| A3 | ~~EAS 프로젝트 미연결~~ **해소** | 사용자가 로그인한 뒤 `eas init --account usunge`로 연결했다(계정 선택은 사용자 결정). `@usunge/hellchang-grow`, projectId `97f8c096-…`, owner `usunge`가 `app.json`에 들어갔고 `npx expo config`로 확인했다 | — |
| A4 | **스토어 계정·서명 키 없음** | 저장소에서 확인 가능한 서명/자격 증명이 없다(정상 — 저장소에 두면 안 된다) | Google Play 개발자 계정, Apple Developer Program, 키스토어/배포 인증서 — **사용자만 가능** |
| A5 | **개인정보처리방침 — 초안 확보, 게시 대기** | [docs/PRIVACY.md](docs/PRIVACY.md) 1부에 코드에서 확인한 사실만으로 초안을 썼다. 운영 주체/연락처/시행일/게시 URL은 `{{...}}` 자리로 비워 뒀다 — 지어내지 않았다 | 남은 것: 운영 주체 정보 채우기 → 법률 검토 → 공개 URL 게시 → 스토어 등재 정보에 입력. **사용자만 가능** |

---

## B. 출시 전 필수 — 제출은 가능하지만 이대로 내면 문제가 된다

| # | 항목 | 확인된 사실 | 조치 |
| --- | --- | --- | --- |
| B1 | ~~쓰지 않는 마이크 권한~~ **해소** | `expo-image-picker` 플러그인에 `microphonePermission: false`, `cameraPermission: false`를 넣었다. 재확인 결과 해석된 설정에 `RECORD_AUDIO`가 **0건**이고 권한 배열 자체가 사라졌다(앱은 사진 라이브러리만 쓴다) | — |
| B2 | ~~앱 표시 이름이 개발용 슬러그~~ **해소** | 사용자 확정으로 `name: "헬창키우기"`(기기 홈 화면·스토어 등재명). `slug`은 EAS 연결 값이라 `hellchang-grow` 그대로 두고, `verify:release`가 이름이 슬러그로 되돌아가는 것을 막는다 | — |
| B3 | ~~버전 정책 없음~~ **해소** | `android.versionCode: 1`, `ios.buildNumber: "1"`을 명시하고 `eas.json`의 `appVersionSource`를 `local`로 뒀다 — 빌드 때 파일이 조용히 바뀌지 않고, 올릴 때만 저장소에서 올린다 | 릴리스마다 수동 증가(자동 증가를 원하면 결정 필요) |
| B4 | ~~선택한 사진이 나중에 사라질 수 있다~~ **해소** | 기록을 저장하는 순간 문서 디렉터리(`body-photos/`)로 복사하고 그 경로를 남긴다(`services/storage/photo-store.ts`). 복사 지점은 `addBodyHistoryEntry` 한 곳이라 온보딩·히스토리 양쪽이 함께 덮인다. 실패하면 원래 URI로 떨어져 기록 저장은 막지 않는다. `expo-file-system`을 승인받아 `package.json`에 선언했다(`~57.0.5`) | 이미 저장된 옛 기록의 사진은 되살릴 수 없다. 실기기 확인은 `E4` |
| B5 | ~~의존성 패치 버전 불일치~~ **해소** | `npx expo install --fix`로 정렬했다(승인). expo 57.0.14→57.0.16, expo-router 57.0.14→57.0.16, expo-image-picker 57.0.11→57.0.13, expo-splash-screen 57.0.7→57.0.8, expo-linking 57.0.6→57.0.7. 전부 SDK 57 안의 패치 버전이다. `expo-doctor` 21/21 | — |
| B6 | ~~쓰지 않는 네이티브 의존성 6개~~ **해소** | 여섯 개를 직접 의존성에서 뺐다(승인). 다만 효과가 갈린다 — `expo-device`/`expo-web-browser`는 요구하는 패키지가 없어 **트리에서 완전히 빠졌고**, `expo-symbols`/`expo-glass-effect`/`@expo/ui`는 `expo-router`가, `expo-font`는 `expo`가 의존해 **여전히 설치·링크된다**(얻은 것은 정직한 manifest). `expo export` 성공으로 빠진 참조가 없음을 확인 | — |
| B7 | **데이터 안전/개인정보 답변 — 시트 확보, 입력 대기** | [docs/PRIVACY.md](docs/PRIVACY.md) 2부에 Play 데이터 안전 / Apple 라벨 답변을 항목별 근거와 함께 정리했다. 확인된 사실: 저장은 전부 기기 로컬(14개 키), 사진은 경로만 저장하고 업로드 경로가 없다, 외부 통신은 AI PT `fetch` 하나뿐이며 현재 **엔드포인트 미설정**(`resolveTrainerEndpointUrl()` → null). AI PT를 켜면 키·체중·체지방률·골격근량과 운동 기록이 전송되므로 답변이 "건강·피트니스 수집"으로 바뀐다 | 스토어 콘솔 입력 — **사용자만 가능** |
| B8 | **크래시 리포팅 없음** | 저장소에 crash/analytics SDK가 없다 | 초기 사용자 문제를 볼 방법이 없다. 도입은 **의존성 추가 = APPROVAL REQUIRED** |

---

## C. 출시 후 가능

| # | 항목 | 상태 |
| --- | --- | --- |
| C1 | 실제 AI PT 연결 | 어댑터와 컨텍스트는 준비됨(`remote-trainer-service`). 연결은 `DEC-003` 결정 사항 |
| C2 | 광고 SDK / 인앱결제 연결 | 경계만 준비됨. 지금은 광고 보상이 아예 나가지 않는다(`FAIL-007` 참조) |
| C3 | 푸시 알림/리마인더 | 앱 안 알림 화면만 있고 `expo-notifications` 미설치 |
| C4 | AI 채팅 화면의 스탠리 이모지 | `ai-chat.tsx`만 이모지, 홈/트레이너 탭은 실제 초상 이미지를 쓴다 |
| C5 | 세트/PR 실제 애니메이션 클립 | 현재는 반동·확대 연출 |
| C6 | ~~`README.md` 템플릿~~ **해소** | 제품 정의·읽는 순서·검증 방법·폴더 구조로 교체했다 |

---

## D. 이미 완료 (근거 있음)

| # | 항목 | 근거 |
| --- | --- | --- |
| D1 | 운동 CORE(세션/타이머/세트/휴식/루틴) | `verify:session` 69, `verify:workout-core` 64, `verify:weight-core` 88 |
| D2 | 성장·신체 상태 파이프라인 | `verify:growth` 68, `verify:body` 87, `verify:growth-reveal` 64 |
| D3 | 세션 완료 idempotency + 손상 receipt 방침 | `verify:core-loop` 85, `DEC-010` |
| D4 | 저장값 손상 방어(모든 키) + 부팅 복구 | `verify:storage` 168, `FAIL-008` |
| D5 | 권리(entitlement) 단일 판정 + 만료 강제 | `verify:entitlement` 55, `DEC-005` |
| D6 | 광고 보상 경계(출시 빌드에 보상 어댑터 없음) | `verify:monetization` 39, `DEC-003`, `FAIL-007` |
| D7 | PR 두 종류(중량/횟수) | `verify:weight-core` 88, `DEC-011` |
| D8 | 캐릭터 CANON 렌더러 + 스탠리 초상 아트 | `verify:character-body` 32, `assets/characters/trainer/stanley_portrait.png`(홈·트레이너 탭에서 사용) |
| D9 | production JS 번들 생성 | `npx expo export --platform android` exit 0, 4.3MB |

---

## E. MANUAL QA — 저장소에서 확인할 수 없다

| # | 항목 | 방법 |
| --- | --- | --- |
| E1 | 세션 중 앱 kill 후 복구 | [docs/DEVICE_QA.md](docs/DEVICE_QA.md) `E1` (원 절차는 `scripts/verify-storage-recovery.ts` 하단) |
| E2 | 아이콘/스플래시가 Expo 템플릿 기본인지 | `assets/images/`에 `react-logo*`, `expo-badge*` 등 템플릿 잔재가 함께 있다 — 실제 아이콘을 눈으로 확인 |
| E3 | 권한 다이얼로그 문구 | 사진 접근 요청 문구가 실기기에서 어떻게 보이는지 |
| E4 | 사진 보관(B4) 확인 | 사진을 넣은 기록을 만든 뒤 며칠 뒤 [몸 변화]에서 다시 열어 그대로 있는지 본다. 복사가 실패하는 기기라면 기록만 남고 사진 자리가 비어야 한다 — 기록 저장 자체가 실패하면 안 된다 |
| E5 | EAS preview 빌드 1회 성공 | A1~A3 해결됨 — Android preview 빌드를 실행했다(빌드 id `626adae7-…`). 설치·실행 확인은 [docs/DEVICE_QA.md](docs/DEVICE_QA.md) |
| E6 | 다양한 화면 크기/저사양 기기 | 세션 화면의 세트 입력·휴식 타이머 중심 |

---

## F. 조사 중 발견한 문서 오류 (이 커밋에서 정정)

`docs/ROADMAP.md`의 M3 현황 메모에 "트레이너 아트 **미완**"이라고 적었으나, 실제로는
`assets/characters/trainer/stanley_portrait.png`가 존재하고 홈·트레이너 탭이 이미 쓰고 있다
(`character-assets.ts`의 `StanleyPortraitImage`). 이모지가 남은 곳은 `ai-chat.tsx` 한 곳이다.
해당 줄을 사실에 맞게 고쳤다.

## G. 관찰 (조치 불필요)

- `PlayerCharacterAssets.home`이 `player_main.png`를 가리키지만, `PlayerCharacter`는 `bodyParameters`가 있으면 **CANON 파라메트릭 렌더러**로 그린다. PNG는 성장 상태가 없는 자리의 fallback이다 — CANON 대체가 아니다.
- 외부 네트워크 호출은 `remote-trainer-service.ts`의 `fetch` 하나뿐이다. 그 외에는 앱이 기기를 벗어나지 않는다.
- API 키는 저장소 어디에도 없다(설계상 프록시 서버만 보유).
