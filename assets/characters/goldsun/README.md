# 골드썬 트레이너 이미지 위치

골드썬(내부 ID: `stanley`) 포트레이트 아트가 준비되면 이 폴더에 넣고
`src/config/character-assets.ts`의 `GoldsunPortraitImage`에 경로를 연결한다.

아직 이 폴더가 비어 있는 동안은 이모지 placeholder(`portraitPlaceholder`,
`src/config/trainers.ts`)를 대신 사용한다.
